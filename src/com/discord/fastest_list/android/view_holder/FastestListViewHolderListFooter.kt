package com.discord.fastest_list.android.view_holder

import android.view.ViewGroup
import com.discord.fastest_list.android.FastestListSections.Entry
import kotlin.jvm.internal.r

internal class FastestListViewHolderListFooter(parent: ViewGroup, onUnexpectedItemSize: (Entry, Int) -> Unit) : FastestListViewHolder {
   init {
      r.h(var1, "parent");
      r.h(var2, "onUnexpectedItemSize");
      super(var1, var2, null);
   }
}
